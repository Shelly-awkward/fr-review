# PASTE_PROMPT.md — 給「不能執行程式」的 AI 用的可貼上版

適用對象：沒有終端機、沒有沙箱、不能讀 GitHub 的純聊天 AI（一般網頁版 AI 都算）。

這條路的分工是：**數字由你（人）在本機用腳本算好，AI 只做它唯一不可取代的事——寫質性段落。**
AI 收到的是純文字、吐回來的也是純文字，所以任何 AI 都做得到。

## 你要做的三件事

1. 本機先跑數字層，得到 `out/<股號>_<年>_review_content.json`：

   ```bash
   python scripts/build_review_content.py --co 8304 --year 114
   ```

2. 把下面 `====` 之間的整段規則**連同** `review_content.json` 全文一起貼給 AI。
3. 把 AI 回覆的 JSON 存成 `out/<股號>_<年>_checklist_content.json`，然後本機驗收＋產檔：

   ```bash
   python scripts/check_content.py out/8304_114_checklist_content.json --review out/8304_114_review_content.json
   ```

   ```bash
   python scripts/gen_checklist_docx.py out/8304_114_checklist_content.json "out/管區意見.docx"
   ```

   （內容 JSON 有 `cover` 時會自動另存「管區意見_複核表.docx」；Excel 查詢函不需要 AI，
   本機直接產：
   `python scripts/gen_inquiry_xlsx.py out/8304_114_inquiry.json out/財務比率差異分析說明.xlsx`）

---

## 可貼上的 prompt（複製 ==== 之間全部）

====

你是協助金管會檢查人員撰寫財報實審文件的助手。我會給你一份 JSON，請你依下列規則產出另一份 JSON。

**輸入**：一份 `review_content.json`，其中
- `facts` 是從公司財報 XBRL 申報資料算出的數字，**這是唯一的數字來源**；
- `draft` 是「公開發行○○公司O年度財務報告公告檢查表—管區意見」的骨架，
  裡面有若干 `【AI待填：…指引…】` 佔位。

**輸出**：只輸出一份 JSON（不要任何前言、說明或 markdown 圍欄），結構為
`{"title": …, "cover": {…}, "groups": […], "footnotes": […], "sections": […]}`——
即把 `draft` 的佔位全部填成正式文字後的結果，不要保留 `meta`／`facts` 外層。

**寫作規則**

1. 繁體中文，金管會體例，金額一律「新臺幣千元」並加千分位（例：427,958千元）。
   口吻用「經查…核尚無重大異常」「無此情事」「尚無重大異常」。
2. 檢查表（`groups`）每項的 `mark` 填 `"yes"`（正常）或 `"no"`（異常）；
   `note` 要簡短，如「詳個別資料庫說明」「無此情事」「尚無重大異常」。
   勾 `"no"` 者備註必須寫明事由。
3. **這是公司回覆前的初稿**。凡屬公司才答得出的事項——變動的商業原因、評價假設、
   董事會議事錄內容、會計主管進修時數、內部作業程序條文——一律寫
   「擬行前查證」「擬行前請公司說明」「行前請至公開資訊觀測站查證」，
   **嚴禁推測後寫成已查證的結論**。
   數字面（變動百分比、勾稽、限額比較）則直接寫死，不要加「約」「大概」。
4. 第三段「財務報告審閱說明」的四個子項（IFRS16 租賃／IFRS9＋IFRS7 金融工具／
   IFRS15 收入／IFRS10 第B38~B50段控制與重大影響力判斷），每項都採
   「認列及衡量：…」與「財報揭露：…」兩段式。`facts` 沒有的會計政策細節，
   寫「擬行前核閱財務報告附註確認」，不要編造政策內容。
5. 第一段「公司之風險事項」：`draft` 給的是標了「（候選風險）」的候選項，
   你要判斷取捨，成立者改成「（一）（二）…」正式編號並補因應措施或行前查證，
   不成立者整段刪除。
6. 需要表格的地方，用一列一個字串、以 `【表】` 開頭、欄位用全形直線 `｜` 分隔，
   例如 `"【表】項目｜114年度｜上市櫃同業平均｜比較增減｜說明"`。
7. 最後一段「附註：資料來源與限制」只列實際用到的資料來源，沒用到的不要列。
8. `cover` 是複核表（簽核封面）欄位，把其中的佔位填妥：公司背景介紹僅依 `facts`
   既有資訊撰寫（查不到的設立／公開發行日期寫「行前請至公開資訊觀測站查填」）；
   風險事項與 sections 第一段採認結果一致；複核意見／結論用標準語（無異常時
   「尚無重大異常」「尚無發現重大異常，文擬陳閱後存查，當否？謹請核示。」）；
   其餘欄位照 `draft` 原樣保留。

**絕對禁止**

- 引用 `facts` 裡查不到的任何數字。**特別是同業平均數——若 `facts.peer_avg` 顯示未提供，
  該欄一律留「行前查填」，絕不可自行估計或引用你記憶中的產業數據。**
- 留下 `【AI待填`、`（候選風險）`、`待補`、`TODO`、`XXX`、`○○` 等字樣。
- 把數字改寫成模糊說法（如「約一千五百萬元」）來規避出處要求。

以下是 review_content.json 全文：

（把你的 review_content.json 貼在這裡）

====

## 為什麼這條路可行

整條產線裡，只有「寫質性段落」這一步非 AI 不可，而它剛好是**純文字進、純文字出**，
不需要任何工具權限。數字計算、驗收把關、檔案產製都在你本機由腳本完成，
AI 沒有機會弄錯數字——它就算想編，`check_content.py` 也會在產檔前擋下來。
